<dec f='remake/makeint.h' l='658' type='int start_remote_job(char ** , char ** , int , int * , int * , int * )'/>
<use f='remake/job.c' l='1262' u='c' c='start_job_command'/>
<def f='remake/remote-stub.c' l='52' ll='58' type='int start_remote_job(char ** argv, char ** envp, int stdin_fd, int * is_remote, int * id_ptr, int * used_stdin)'/>
<doc f='remake/remote-stub.c' l='45'>/* Start a remote job running the command in ARGV,
   with environment from ENVP.  It gets standard input from STDIN_FD.  On
   failure, return nonzero.  On success, return zero, and set *USED_STDIN
   to nonzero if it will actually use STDIN_FD, zero if not, set *ID_PTR to
   a unique identification, and set *IS_REMOTE to zero if the job is local,
   nonzero if it is remote (meaning *ID_PTR is a process ID).  */</doc>
