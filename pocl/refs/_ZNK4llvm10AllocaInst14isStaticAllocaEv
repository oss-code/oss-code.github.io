<dec f='include/llvm-6.0/llvm/IR/Instructions.h' l='117' type='bool llvm::AllocaInst::isStaticAlloca() const'/>
<doc f='include/llvm-6.0/llvm/IR/Instructions.h' l='114'>/// Return true if this alloca is in the entry block of the function and is a
  /// constant size. If so, the code generator will fold it into the
  /// prolog/epilog code, so it is basically free.</doc>
