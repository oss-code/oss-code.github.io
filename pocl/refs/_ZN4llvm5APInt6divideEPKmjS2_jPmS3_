<dec f='include/llvm-6.0/llvm/ADT/APInt.h' l='185' type='static void llvm::APInt::divide(const WordType * LHS, unsigned int lhsWords, const WordType * RHS, unsigned int rhsWords, WordType * Quotient, WordType * Remainder)'/>
<doc f='include/llvm-6.0/llvm/ADT/APInt.h' l='179'>/// \brief An internal division function for dividing APInts.
  ///
  /// This is used by the toString method to divide by the radix. It simply
  /// provides a more convenient form of divide for internal use since KnuthDiv
  /// has specific constraints on its inputs. If those constraints are not met
  /// then it provides a simpler form of divide.</doc>
