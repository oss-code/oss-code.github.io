<def f='include/llvm-6.0/llvm/Option/ArgList.h' l='159' ll='163' type='void llvm::opt::ArgList::ArgList(llvm::opt::ArgList &amp;&amp; RHS)'/>
<use f='include/llvm-6.0/llvm/Option/ArgList.h' l='396' u='c' c='_ZN4llvm3opt12InputArgListC1EOS1_'/>
<doc f='include/llvm-6.0/llvm/Option/ArgList.h' l='154'>// Explicit move operations to ensure the container is cleared post-move
  // otherwise it could lead to a double-delete in the case of moving of an
  // InputArgList which deletes the contents of the container. If we could fix
  // up the ownership here (delegate storage/ownership to the derived class so
  // it can be a container of unique_ptr) this would be simpler.</doc>
